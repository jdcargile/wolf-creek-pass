# UDOT Traffic API key -- https://www.udottraffic.utah.gov/developers/doc
UDOT_API_KEY=

# Storage backend: "dynamo" (DynamoDB + S3) or "sqlite" (local SQLite + filesystem)
# Use "sqlite" for offline dev (no Docker needed), "dynamo" for LocalStack/AWS
STORAGE_BACKEND=sqlite

# AWS config (for LocalStack, set AWS_ENDPOINT_URL=http://localhost:4566)
# AWS_ENDPOINT_URL=http://localhost:4566
AWS_DEFAULT_REGION=us-east-1
# AWS_ACCESS_KEY_ID=test
# AWS_SECRET_ACCESS_KEY=test

# Vue frontend data URL (optional, overrides Vite dev server plugin)
# For LocalStack: VITE_DATA_URL=http://localhost:4566/wolf-creek-pass/data
# For production: set to S3 bucket URL (auto-configured by CDK)

# Reolink cabin camera API (Lambda Function URL -- set after CDK deploy)
# VITE_REOLINK_API_URL=https://xxxx.lambda-url.us-east-1.on.aws/

# SensorPush credentials (for local dev server and Lambda)
# SENSORPUSH_EMAIL=your_email@example.com
# SENSORPUSH_PASSWORD=your_password_here
